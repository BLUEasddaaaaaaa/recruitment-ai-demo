# 招聘 AI Demo 面试官使用说明

## 1. 项目简介

这是一个可本地运行的招聘 AI 应用 Demo，用于解决 HR 在招聘流程中手工记录、候选人跟进、面试信息沉淀和招聘进展汇总效率低的问题。

当前版本为：

```text
React 前端 + Python FastAPI 后端 + SQLite 本地数据库
```

核心能力：

- AI 简历解析与候选人信息自动预填
- HR 人工确认入库，避免 AI 错误直接写入
- 候选人招聘流程台账
- 岗位 / 部门 / 状态 / HR 多维筛选
- 面试轮次与评价记录追加
- AI 招聘运营助手
- 自动生成今日待办、运营洞察和企业微信汇报文案

代码地址：

```text
https://github.com/BLUEasddaaaaaaa/recruitment-ai-demo
```

---

## 2. 方式一：在线预览（推荐，无需安装）

直接打开下面链接即可展示页面效果：

```text
https://b7cd2e6fa96b401cb84c95256973c33b.app.codebuddy.work
```

该链接为纯前端演示模式：

- 已内置示例简历和示例候选人数据
- 无需 Python 后端、无需数据库、无需安装依赖
- 手机和电脑浏览器都能直接打开
- 独立交互展示页：`https://b7cd2e6fa96b401cb84c95256973c33b.app.codebuddy.work/demo.html`

> 注意：在线预览是静态演示页，数据保存在浏览器内存中，刷新后恢复到初始示例数据。如需持久化数据，请使用下方本地完整版启动方式。

---

## 3. 方式二：本地完整版（安装依赖后一键启动）

如需展示真实后端、数据库持久化、简历文件解析，请使用本地完整版。

### 3.1 环境要求

- Python 3.12+
- Node.js 18+
- npm 或 pnpm
- 浏览器

macOS 用户可直接使用项目内的一键启动脚本。

### 3.2 一键启动

下载或 clone 项目后，进入项目根目录：

```bash
cd recruitment-ai-demo
```

macOS 下直接双击：

```text
启动完整招聘AI-Demo.command
```

或在终端执行：

```bash
./启动完整招聘AI-Demo.command
```

脚本会自动完成：

1. 创建 Python 虚拟环境
2. 安装 Python 依赖
3. 安装前端依赖
4. 启动 FastAPI 后端
5. 启动 React 前端
6. 自动打开浏览器

> **首次运行请注意**：脚本会自动创建 Python 虚拟环境并安装前后端依赖，这一步可能需要 **30–60 秒**。Terminal 窗口里看起来会停一会儿，但**不要关闭窗口**，等看到「打开前端页面：http://localhost:3000」才算启动完成。后续再启动只需几秒。

启动后访问：

```text
http://localhost:3000
```

后端健康检查：

```text
http://localhost:8000/api/health
```

如果返回：

```json
{"status":"ok"}
```

说明后端正常运行。

### 3.3 手动启动（备用）

如果一键脚本不可用，可以分别启动前后端。

**启动后端：**

```bash
cd recruitment-ai-demo

python3 -m venv .venv
source .venv/bin/activate

pip install -e .

export AI_MODE=fake
uvicorn src.api.main:app --reload --port 8000
```

**启动前端：**

另开一个终端：

```bash
cd recruitment-ai-demo/frontend

npm install --registry=https://registry.npmmirror.com
npm run dev
```

访问：

```text
http://localhost:3000
```

---

## 4. Demo 演示流程

### 4.1 简历智能录入

进入“简历智能录入工作区”。

推荐操作：

1. 使用页面内置示例简历，或粘贴测试简历文本。
2. 点击“开始 AI 智能提取”。
3. 查看系统自动提取的姓名、电话、邮箱、岗位、部门、教育经历、技能等字段。
4. 查看字段置信度和证据提示。
5. HR 可人工修改 AI 预填字段。
6. 填写负责 HR。
7. 点击“确认入库”。

展示重点：

- AI 自动提取候选人结构化信息
- HR 人工确认，避免错误入库
- 入库后数据写入 SQLite，刷新不丢失

### 4.2 候选人流程管理

进入“招聘台账 / 候选人流程”页面。

页面能力：

- 表格展示所有候选人
- 支持关键词搜索
- 支持按岗位筛选
- 支持按部门筛选
- 支持按流程状态筛选
- 支持按 HR 筛选

推荐操作：

1. 使用筛选器查找候选人。
2. 点击候选人表格行。
3. 在详情面板查看候选人基础信息、简历信息和历史面试记录。
4. 更新候选人流程状态。
5. 追加一轮面试记录，包括面试官、评价和结论。

展示重点：

- 替代 HR 手工维护招聘台账
- 候选人流程状态可追踪
- 面试记录持续追加，不覆盖历史
- 前端操作会实时写入 Python 后端和 SQLite

### 4.3 AI 招聘运营助手

进入“AI 招聘运营助手”页面。

页面包含：

1. 今日待办
2. AI 运营洞察
3. 一键生成企业微信汇报

展示重点：

- 系统根据招聘流程数据自动生成 HR 今日待办
- 自动总结招聘流程卡点和重点岗位
- 自动生成可复制到企业微信群的招聘进展汇报

---

## 5. AI 模式说明

默认运行模式为：

```bash
AI_MODE=fake
```

该模式无需 API Key，适合本地演示。

它会使用离线模拟 AI 能力展示：

- 简历字段提取
- 候选人信息预填
- 招聘运营洞察
- 企业微信汇报生成

如需接入真实 OpenAI 模型，可配置：

```bash
export AI_MODE=real
export OPENAI_API_KEY="your_api_key"
```

然后重新启动后端。

---

## 6. 后续接入方向：企业微信与腾讯文档

当前 Demo 已内置“生成企业微信汇报文案”的能力。后续若要真正接入企业微信和腾讯文档，可按以下思路扩展。

### 6.1 企业微信接入

**适用场景：**

- 候选人状态变更时，自动推送消息到 HR 群
- 每日定时发送招聘日报到指定企业微信群
- 面试官面试前自动提醒

**接入方式：**

1. 注册企业微信自建应用
   - 登录 [企业微信管理后台](https://work.weixin.qq.com/wework_admin/loginpage_wx)
   - 应用管理 → 创建自建应用，获取 `corpid`、`agentid`、`secret`

2. 后端新增消息推送模块
   - 在 `src/api/` 下新增 `wecom.py` 服务
   - 调用企业微信「发送应用消息」接口：`POST https://qyapi.weixin.qq.com/cgi-bin/message/send?access_token=ACCESS_TOKEN`
   - 先调 `gettoken` 接口获取 `access_token`（`corpid` + `secret`）

3. 关键触发点
   - 候选人状态更新后触发（在 `PUT /api/applications/{id}/status` 中调用）
   - 每日定时任务生成招聘日报并推送（可用 `APScheduler` 或 Linux `cron`）

4. 简单示例代码

```python
import requests

def send_wecom_message(corpid, secret, agentid, msg):
    # 1. 获取 token
    token_url = f"https://qyapi.weixin.qq.com/cgi-bin/gettoken?corpid={corpid}&corpsecret={secret}"
    token = requests.get(token_url).json()["access_token"]

    # 2. 发送文本消息
    send_url = f"https://qyapi.weixin.qq.com/cgi-bin/message/send?access_token={token}"
    payload = {
        "touser": "@all",          # 或指定成员 UserID
        "msgtype": "text",
        "agentid": agentid,
        "text": {"content": msg},
        "safe": 0
    }
    return requests.post(send_url, json=payload).json()
```

5. 面试提醒可改用 `markdown` / `news` 类型，带上候选人链接和面试时间。

> 注意：企业微信需要企业资质或开发者测试号；个人可先用「企业微信 · 开发者工具」或「应用消息」沙箱模式调试。

### 6.2 腾讯文档接入

**适用场景：**

- 将候选人信息自动写入腾讯文档表格，替代 Excel 手工台账
- 面试记录同步到协作文档，方便面试官查看
- 招聘日报自动生成在线文档并分享到企业微信

**接入方式：**

1. 准备腾讯文档 / 腾讯生态接入能力
   - 使用 [腾讯文档开放平台](https://docs.qq.com/open) 或 [腾讯文档 API](https://docs.qq.com/static/pdf/腾讯云API/腾讯文档开放平台接入指南.pdf) 创建应用
   - 申请 `client_id`、`client_secret`，获取 access_token

2. 后端新增腾讯文档服务模块
   - 在 `src/api/` 下新增 `tencent_docs.py`
   - 封装「创建表格」「追加记录」「读取表格」接口

3. 常见对接模式

**模式 A：写入现有表格**

- 手动在腾讯文档中创建一个「招聘台账」智能表格
- 记录该表格的 `sheet_id` 和字段顺序（姓名、岗位、部门、状态、HR、更新时间）
- 后端在候选人入库或状态更新时，调用 API 追加或更新一行记录

```python
import requests

def append_to_tencent_sheet(access_token, sheet_id, row_values):
    url = f"https://docs.qq.com/api/v1/sheets/{sheet_id}/append_rows"
    headers = {"Authorization": f"Bearer {access_token}"}
    payload = {"values": [row_values]}
    return requests.post(url, headers=headers, json=payload).json()
```

**模式 B：自动生成招聘日报文档**

- 后端调用「创建文档」接口生成一个日报在线文档
- 将 AI 生成的日报内容写入正文
- 返回文档链接，可一键转发到企业微信

4. 关键触发点
   - 候选人入库 → 追加到腾讯文档台账
   - 状态变更 → 更新对应行
   - 每日定时 → 生成日报文档并推送链接

### 6.3 推荐扩展顺序

| 阶段 | 目标 | 改动点 |
|------|------|--------|
| 第一阶段 | 企业微信文本推送 | 后端新增 `wecom.py`，在状态变更和定时任务中调用 |
| 第二阶段 | 腾讯文档台账写入 | 后端新增 `tencent_docs.py`，候选人入库时追加记录 |
| 第三阶段 | 日报文档 + 群推送 | 定时任务生成日报文档，获取链接后推送到企业微信 |

> 当前 Demo 已预留了 AI 运营日报文案生成能力，后续只需把「复制文案」改为「调用 wecom / 腾讯文档 API 发送」即可。

---

## 7. 数据说明

本 Demo 使用 SQLite 本地数据库。

默认数据库文件：

```text
data/recruitment.db
```

刷新页面后，已入库候选人和面试记录不会丢失。

如需重置演示数据，可停止服务后删除：

```text
data/recruitment.db
```

然后重新启动应用。

---

## 8. 常见问题

### 8.1 前端打不开

请确认前端地址：

```text
http://localhost:3000
```

同时确认后端健康检查正常：

```text
http://localhost:8000/api/health
```

如果后端正常但前端打不开，请检查终端脚本是否还在运行、是否已提示「打开前端页面：http://localhost:3000」。也可以查看日志文件：

```text
data/frontend.log
data/backend.log
```

若日志里出现 `tsx: command not found` 等依赖错误，请确认首次运行时脚本已完整执行完依赖安装（约 30–60 秒），或在项目根目录重新执行：

```bash
cd recruitment-ai-demo
source .venv/bin/activate
pip install -e .
cd frontend
npm install --registry=https://registry.npmmirror.com
npm run dev
```

### 8.2 后端启动失败

请确认 Python 版本为 3.12 或以上，并已安装依赖：

```bash
pip install -e .
```

### 8.3 端口被占用

如果 `3000` 或 `8000` 被占用，可以修改端口。

后端示例：

```bash
uvicorn src.api.main:app --reload --port 8080
```

然后在前端 `.env` 中设置：

```env
VITE_API_BASE_URL=http://localhost:8080
```

### 8.5 双击 `.command` 提示「Apple 无法验证…是否包含恶意软件」

这是 macOS 的 **Gatekeeper 安全机制**，不是文件真的有问题。因为从 zip 或网络下载的 `.command` 脚本没有苹果开发者签名，系统会默认拦截。

**解决方法 1：右键打开（最简单）**

1. 找到 `启动完整招聘AI-Demo.command`
2. 按住键盘 **Control** 键，同时用鼠标**点一下这个文件**（或右键点击）
3. 在菜单中选择 **「打开」**
4. 弹出窗口后，点击 **「仍要打开」** 按钮
5. 之后这个文件就可以正常双击运行了

**解决方法 2：移除隔离属性（推荐，一劳永逸）**

如果方法 1 无效，打开终端（Terminal.app），粘贴并执行下面命令：

```bash
xattr -d com.apple.quarantine /path/to/启动完整招聘AI-Demo.command
```

示例（如果项目放在当前下载位置）：

```bash
xattr -d com.apple.quarantine /Users/molan/Documents/Codex/2026-07-19/hr-ai/outputs/recruitment-ai-demo/启动完整招聘AI-Demo.command
```

> **命令说明**：`xattr -d` 表示删除扩展属性；`com.apple.quarantine` 是系统给外部下载文件加的隔离标记。删除后，macOS 就不会再拦截这个脚本。
>
> 如果终端提示「Operation not permitted」，说明终端没有磁盘权限。请去 **系统设置 → 隐私与安全性 → 完全磁盘访问权限**，把 **Terminal** 加入并开启开关，然后重新打开终端执行。

**解决方法 3：直接通过终端运行（绕过双击拦截）**

复制下面命令到终端，按回车即可启动：

```bash
zsh /Users/molan/Documents/Codex/2026-07-19/hr-ai/outputs/recruitment-ai-demo/启动完整招聘AI-Demo.command
```

如果解压后项目带了空格路径（如 `recruitment-ai-demo 3`），空格前要加反斜杠 `\`：

```bash
zsh /Users/molan/Documents/Codex/2026-07-19/hr-ai/outputs/recruitment-ai-demo\ 3/启动完整招聘AI-Demo.command
```

**发给别人时怎么避免这个问题**

把 zip 包发给面试官或同学时，他们解压后大概率也会遇到同样的 Gatekeeper 提示。建议在发送时附带一句说明：

> 解压后双击 `.command` 文件时，若 macOS 提示「无法验证」，请按住 Control 键点文件 → 打开 → 点击「仍要打开」。

---

## 9. 推荐演示话术

可以这样介绍：

> 这个 Demo 解决的是 HR 招聘流程中大量手工记录、跟踪和汇总的问题。HR 获取简历后，系统通过 AI 自动解析候选人信息，并由 HR 确认后入库。后续候选人进入招聘流程台账，可进行部门筛选、面试记录追加和状态跟进。系统还会基于流程数据自动生成今日待办、AI 运营洞察和企业微信汇报文案，从而减少 HR 的重复录入和手工汇总工作。

---

## 10. 当前状态

当前版本适合用于：

- 本地演示
- 面试展示
- 课程 / 项目答辩
- AI 应用原型展示

当前版本不建议直接用于生产环境。

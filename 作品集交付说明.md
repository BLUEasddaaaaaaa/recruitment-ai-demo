# 招聘 AI 应用 Demo · 作品集交付说明

> 本文件用于回填你的**个人作品集网页**中「AI 应用 Demo」条目。
> 一个完整的作品集条目需要三件套：**可直接运行** + **代码链接** + **在线预览**。

---

## 一、条目一句话简介（可直接贴作品集）

> **智能招聘 AI 助手** —— 一个全栈 AI 应用 Demo：上传简历即由 AI 自动抽取姓名/岗位/技能等结构化字段并预填入库，支持候选人流程状态流转、面试记录沉淀，以及基于全量台账自动生成的「今日待办 + AI 洞察 + 企业微信简报」。前端 React + Tailwind，后端 FastAPI + SQLite + AI Provider（含离线 fake 模式）。

---

## 二、三件套交付清单

### 1️⃣ 可直接运行（全栈 · 含真实后端）

代码本身就是一个**真实可跑的全栈应用**，不是截图或视频：

- **后端**：FastAPI 提供 7 个 REST 接口（简历解析、候选人增删改查、流程状态、面试记录、AI 大盘），数据落在本地 SQLite。
- **AI 能力**：接入 AI Provider 做简历字段抽取与运营洞察；内置 `fake` 离线模式，**无需任何 API Key 也能完整跑通全部功能**，适合演示。
- **前端**：React + Tailwind 实现的深色仪表盘 UI，含简历录入、候选人流转、AI 运营助手三大模块。

**运行方式（二选一）：**

- **方式 A（推荐，零命令）**：双击项目里的 `启动完整招聘AI-Demo.command`，自动建环境、装依赖、同时拉起后端(`:8000`)与前端(`:3000`)并打开浏览器。
- **方式 B（手动命令）**：
  ```bash
  cd recruitment-ai-demo
  python3 -m venv .venv
  .venv/bin/pip install -e .
  .venv/bin/uvicorn src.api.main:app --port 8000 &
  cd frontend && npm install && npm run dev
  ```
  然后浏览器打开 http://localhost:3000

---

### 2️⃣ 代码链接（指向你的 GitHub 仓库）

代码已整理就绪（已配置 `.gitignore`，不会误提交 `node_modules` / `.venv` / `dist` / `data` 等）。

> ⚠️ 当前环境无法替你 push 到 GitHub（无 `gh`、无仓库授权），以下步骤需你在自己电脑的终端执行一次，即可生成链接。

```bash
# 进入项目目录（仓库根在 recruitment-ai-demo 的上一级 hr-ai）
cd /Users/molan/Documents/Codex/2026-07-19/hr-ai

# 若尚未初始化（上级已是 git 仓库则可跳过 init）
git add recruitment-ai-demo
git commit -m "feat: 招聘 AI 应用 Demo（全栈 + 演示模式）"

# 在你的 GitHub 新建一个空仓库，拿到地址后：
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

push 成功后，`https://github.com/<你的用户名>/<仓库名>` 即**代码链接**，填进作品集条目的「源码 / Code」处。

---

### 3️⃣ 在线预览（公网可访问的演示 URL）

项目内置 **演示模式（demo mode）**：纯前端、内置示例候选人/解析结果/AI 大盘数据，**完全不依赖后端**，因此可当作静态站点托管到任意平台，拿到永久公网链接。

> 适合放进作品集条目的「Live Demo / 在线预览」：面试官点开即看到完整 UI 与交互流程，无需你电脑开机。

**方式 A · Netlify Drop（最省事，30 秒，无需 git）**

1. 打开 https://app.netlify.com/drop
2. 把本项目的 `frontend/dist` 文件夹**整个拖进去**
3. 自动得到 `https://xxxx.netlify.app` 公网链接

（已内置 `netlify.toml` + `public/_redirects`，刷新不会 404。）

**方式 B · Vercel**

1. 打开 https://vercel.com/new
2. 导入你的 GitHub 仓库（或拖拽 `frontend/dist`）
3. Framework 选 `Other`，Build Command 留空，Output 目录填 `frontend/dist`
4. 部署后得到 `https://xxxx.vercel.app`

> 注：演示模式用的是内置假数据；想要「连真实后端的在线版」需把 FastAPI 也一起部署（CloudStudio / Render 等全栈平台），复杂度更高。作品集展示用演示模式已足够体现产品形态。

---

## 三、技术栈速览（作品集可贴）

| 层 | 技术 |
|---|---|
| 前端 | React 18 · TypeScript · Tailwind CSS · Vite |
| 后端 | Python · FastAPI · SQLModel · SQLite |
| AI | AI Provider 抽象层（真实 LLM / 离线 fake 双模式） |
| 文档解析 | python-docx · pypdf（DOCX / PDF 真实文本抽取） |
| 工程 | 一键启动脚本 · 演示模式 · CORS |

---

## 四、给面试官的演示要点（建议讲这几句）

1. **"上传即解析"**：把一份 DOCX/PDF/TXT 简历丢进去，AI 自动抽字段、预填表单，HR 只需核对微调——讲清楚**减少重复录入**这个价值。
2. **"流程可追踪"**：候选人从筛选→面试→Offer 状态流转，每次状态变更和面试记录都沉淀进结构化台账。
3. **"AI 运营视角"**：底部 AI 运营助手基于全量数据自动生成今日待办、堵点洞察、企微简报——讲清楚**从「记录」到「洞察」的跃迁**。
4. **"工程完整度"**：前后端分离、REST 接口、演示/真实双模式、一键启动——体现全栈交付能力。

---

## 五、本地预览（当前已在运行）

本机已起一个演示模式预览：**http://localhost:4173**（等同上面的「在线预览」本地版，刷新即开）。
全栈真实版（连后端）：**http://localhost:3000**（需先双击 `启动完整招聘AI-Demo.command`）。

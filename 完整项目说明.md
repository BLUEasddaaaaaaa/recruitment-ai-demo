# 招聘 AI Demo 完整项目说明

## 包含内容

本项目已完成前后端整合：

1. **AI Studio React 前端**（`frontend/`）：企业级招聘工作台界面（简历录入、候选人全流程、AI 招聘运营助手）。
2. **Codex Python 后端**（`src/`）：简历提取、AI/规则解析、候选人入库、流程管理、面试记录、AI 运营助手、SQLite 持久化。
3. **FastAPI REST 接口层**（`src/api/`，本次新增）：把上面的 Python 能力以 REST API 暴露给前端。
4. **一键启动脚本**：`启动完整招聘AI-Demo.command`（macOS），同时拉起前后端。

前端不再依赖 mock 数据，所有列表、详情、解析、筛选、面试、运营助手均来自 Python 后端与 SQLite。

## 快速开始

双击 `启动完整招聘AI-Demo.command`（macOS），或按 `README.md` 手动启动：

```bash
# 后端
cd recruitment-ai-demo
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
AI_MODE=fake uvicorn src.api.main:app --reload --port 8000

# 前端
cd frontend
pnpm install && pnpm run dev
```

访问 `http://localhost:3000`。

## 推荐演示顺序

1. **简历智能录入**：用内置预设简历或粘贴文本，点击「开始 AI 智能提取」，核对字段后「确认入库」。
2. **候选人全流程**：在台账中按岗位/部门/状态/HR 筛选；点击某行查看详情、更新筛选状态、追加面试记录（历史不覆盖）。
3. **AI 招聘运营助手**：查看 AI 基于真实数据生成的今日待办、运营洞察与一键企业微信汇报。

## 接口对照

| 前端功能 | 后端接口 |
|---|---|
| 简历 AI 解析 | `POST /api/resume/parse` |
| 确认入库 | `POST /api/candidates` |
| 候选人列表 | `GET /api/candidates` |
| 候选人详情 | `GET /api/candidates/{id}` |
| 更新筛选状态 | `PUT /api/applications/{id}/status` |
| 追加面试记录 | `POST /api/applications/{id}/interviews` |
| AI 运营助手 | `GET /api/ai/dashboard` |

## 文档

- `README.md`：完整运行说明。
- `docs/aistudio-frontend-summary.md`：AI Studio 前端设计整理。
- `INTEGRATION_GUIDE.md`：企业微信 / 腾讯文档集成方案。

> 旧版 Streamlit 入口（`src/app.py`、`scripts/start-demo.command`）仍保留可用，但整合后的主演示为 React + FastAPI 版本。
